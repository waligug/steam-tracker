{
  "name": "steam-tracker",
  "version": "1.0.0",
  "description": "MCP server for tracking upcoming Steam releases by tag with traction signals",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "snapshot": "node dist/index.js --snapshot"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
